<%- include('partials/head') %>
    <%- include('partials/header') %>

        <%- include('partials/head') %>
            <%- include('partials/header') %>

                <!-- Full Screen Feed Layout -->
                <main class="h-[calc(100vh-6rem)] w-full overflow-hidden relative">

                    <!-- Context Header (Top Layer) -->
                    <div class="absolute top-4 left-4 z-50 pointer-events-none">
                        <div class="glass-panel px-6 py-3 rounded-full flex items-center gap-3">
                            <div class="text-2xl">
                                <%= savedMode ? '💖' : '🏛️' %>
                            </div>
                            <div>
                                <h1 class="text-lg font-bold leading-none">
                                    <%= savedMode ? 'Salvos' : 'Feed Personalizado' %>
                                </h1>
                                <p class="text-[10px] opacity-60 uppercase tracking-wider font-semibold">
                                    <%= savedMode ? 'Seus Favoritos' : 'Oportunidades IA' %>
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- Controls (Top Right) -->
                    <div class="absolute top-4 right-4 z-50 flex gap-2">
                        <% if (!savedMode) { %>
                            <a href="/licitacoes/saved" class="btn btn-circle btn-ghost glass tool-btn"
                                title="Ver Salvos">
                                <i class="fas fa-heart text-error"></i>
                            </a>
                            <% } %>
                                <a href="/profile/preferences" class="btn btn-circle btn-ghost glass tool-btn"
                                    title="Preferências">
                                    <i class="fas fa-cog text-primary"></i>
                                </a>
                    </div>

                    <!-- Story Mode Container -->
                    <div id="story-mode" class="h-full w-full">
                        <%- include('partials/story_feed', { licitacoes }) %>
                    </div>

                </main>

                <script>
            // Global Save/                        
            function saveOpportunity(                              fetch(`/api/licitacoes/${id}/s                                    method: 'POST',
                               der                    t-Type': 'application/json' }
                        })
                    .then(                        ))
                    .then(data => {                                 showToast(d                            portunidade salva!', 'success');
                                                                      => {
                                                o ao salvar', 'e                                        console.error(err);
                    });
                                        ion                            type) {
                                        cument.createElement('div');
                                           alert alert-${type}                             1/2                     -1/                    -w-[300px] z-[100] shadow-lg animate                        2xl border-0 bg-opacity-90 backdrop-blur`;
                          ast.innerHTML = `<span class="font-bold flex items-center gap-2">${message}</span>`;
                document.                        (toast);

                setTimeout(() => {
                            toast.classList.add('opacity-0'                        acity');
                                      (() => toast.remove(), 300);
                }, 3000);
            }
                </script>

                <style>
                    .glass-panel {
                        background: rgba(255, 255, 255, 0.1);
                        backdrop-filter: blur(10px);
                        border: 1px solid rgba(255, 255, 255, 0.2);
                        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
                    }

                    .tool-btn:hover {
                        transform: scale(1.1);
                        background: rgba(255, 255, 255, 0.2);
                    }
                </style>

                <!-- Footer Hidden in Feed Mode for Immersion -->