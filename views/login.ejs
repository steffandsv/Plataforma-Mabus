<!DOCTYPE html>
<html lang="pt-BR" data-theme="neuro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MABUS | Access Portal</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/liquid.css">
    <link rel="stylesheet" href="/css/liquid-studio.css">
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-cosmic-void min-h-screen flex items-center justify-center relative overflow-hidden selection:bg-neon-purple selection:text-white">

    <!-- Background Animation -->
    <div class="absolute inset-0 z-0">
        <div class="absolute top-0 left-1/4 w-96 h-96 bg-neon-purple/20 rounded-full blur-[128px] animate-pulse-slow"></div>
        <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-neon-blue/20 rounded-full blur-[128px] animate-pulse-slow" style="animation-delay: 2s;"></div>
    </div>

    <!-- Main Card -->
    <div class="relative z-10 w-full max-w-md p-8 glass-card animate-float">
        <div class="text-center mb-8">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-gradient-to-br from-neon-blue/20 to-neon-purple/20 border border-white/10 mb-4 shadow-[0_0_30px_rgba(0,240,255,0.3)]">
                <i class="fas fa-cube text-3xl text-transparent bg-clip-text bg-gradient-to-r from-neon-blue to-neon-purple"></i>
            </div>
            <h1 class="text-4xl font-bold text-white tracking-tight mb-2">MABUS</h1>
            <p class="text-starlight/60 text-sm tracking-widest uppercase">The Dopamine Engine</p>
        </div>

        <% if (messages.error) { %>
            <div class="alert alert-error mb-6 bg-error/10 border-error/20 text-error shadow-lg backdrop-blur-md">
                <i class="fas fa-exclamation-triangle"></i>
                <span class="font-medium"><%= messages.error %></span>
            </div>
        <% } %>

        <form action="/login" method="POST" class="space-y-6">
            <div class="form-group">
                <label class="text-xs font-bold text-starlight/70 uppercase tracking-wider ml-1 mb-2">Email</label>
                <div class="relative group">
                    <i class="fas fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-starlight/30 group-focus-within:text-neon-blue transition-colors"></i>
                    <input type="email" name="email" class="input input-lg w-full bg-black/20 border border-white/10 focus:border-neon-blue focus:bg-black/40 pl-12 rounded-xl text-starlight placeholder:text-starlight/20 transition-all duration-300" placeholder="user@mabus.ai" required>
                </div>
            </div>

            <div class="form-group">
                <label class="text-xs font-bold text-starlight/70 uppercase tracking-wider ml-1 mb-2">Password</label>
                <div class="relative group">
                    <i class="fas fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-starlight/30 group-focus-within:text-neon-purple transition-colors"></i>
                    <input type="password" name="password" class="input input-lg w-full bg-black/20 border border-white/10 focus:border-neon-purple focus:bg-black/40 pl-12 rounded-xl text-starlight placeholder:text-starlight/20 transition-all duration-300" placeholder="••••••••" required>
                </div>
            </div>

            <button type="submit" class="btn btn-lg w-full bg-gradient-to-r from-neon-blue to-neon-purple hover:opacity-90 border-none text-white font-bold tracking-wider shadow-[0_0_20px_rgba(112,0,255,0.4)] hover:shadow-[0_0_40px_rgba(112,0,255,0.6)] transition-all duration-300 transform hover:-translate-y-1">
                ACCESS SYSTEM
            </button>
        </form>

        <div class="mt-8 text-center">
            <p class="text-starlight/40 text-sm">
                Don't have access? 
                <a href="/register" class="text-neon-blue hover:text-neon-purple transition-colors font-medium ml-1">Initialize Protocol</a>
            </p>
        </div>
    </div>

    <div class="absolute bottom-4 text-center w-full">
        <p class="text-[10px] text-starlight/20 uppercase tracking-[0.2em]">S E C U R E &nbsp; C O N N E C T I O N</p>
    </div>

</body>
</html>
