<% /* DOPAMINE ENGINE v2 - THE VOID STREAM */ %>

    <div class="stream-container">
        <div class="stream-track">
            <% if (!licitacoes || licitacoes.length===0) { %>
                <div class="stream-card flex flex-col items-center justify-center text-center">
                    <div class="text-6xl mb-6 opacity-50">🌑</div>
                    <h3 class="text-2xl font-bold mb-2">O Vazio</h3>
                    <p class="text-base-content/70 mb-6">Não encontramos nada nesta frequência.</p>
                    <a href="/profile/preferences" class="btn btn-primary btn-outline rounded-full">
                        Ajustar Sensores
                    </a>
                </div>
                <% } else { %>
                    <% licitacoes.forEach((lic, index)=> { %>
                        <div class="stream-card" data-id="<%= lic.id %>" data-index="<%= index %>">

                            <!-- 1. Header -->
                            <div class="card-header">
                                <div class="org-badge">
                                    <div class="org-avatar">
                                        <%= (lic.razao_social_orgao || 'X' ).charAt(0).toUpperCase() %>
                                    </div>
                                    <span class="org-name truncate max-w-[200px]">
                                        <%= lic.razao_social_orgao || 'Órgão Desconhecido' %>
                                    </span>
                                </div>
                                <div class="card-meta">
                                    <% if (lic.modalidade_licitacao) { %>
                                        <span class="meta-pill">
                                            <%= lic.modalidade_licitacao %>
                                        </span>
                                        <% } %>
                                            <% if (lic.uf_sigla) { %>
                                                <span class="meta-pill">
                                                    <%= lic.uf_sigla %>
                                                </span>
                                                <% } %>
                                </div>
                            </div>

                            <!-- 2. The Value (Hero) -->
                            <div class="card-value-section">
                                <span class="value-label">Potencial Estimado</span>
                                <span class="value-amount">
                                    <% if (lic.is_valor_sigiloso) { %>
                                        Sigiloso 🔒
                                        <% } else { %>
                                            R$ <%= parseFloat(lic.valor_estimado_total || 0).toLocaleString('pt-BR',
                                                {minimumFractionDigits: 0, maximumFractionDigits: 0}) %>
                                                <% } %>
                                </span>

                                <!-- Mini Deadline Indicator -->
                                <% if (lic.data_encerramento_proposta) { %>
                                    <div class="mt-4 flex justify-center items-center gap-2 text-xs opacity-60">
                                        <i class="fas fa-clock"></i>
                                        <span>Até <%= new
                                                Date(lic.data_encerramento_proposta).toLocaleDateString('pt-BR',
                                                {day: '2-digit' , month: '2-digit' , hour: '2-digit' , minute: '2-digit'
                                                }) %></span>
                                    </div>
                                    <% } %>
                            </div>

                            <!-- 3. Object (Content) -->
                            <div class="card-body">
                                <p class="line-clamp-4">
                                    <%= lic.objeto_compra || 'Objeto não informado.' %>
                                </p>
                            </div>

                            <!-- 4. Actions -->
                            <div class="card-actions">
                                <button class="action-btn btn-skip" onclick="skipCard()" title="Pular">
                                    <i class="fas fa-times"></i>
                                </button>
                                <a href="/licitacoes/<%= lic.id %>" class="action-btn btn-details" title="Detalhes">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <button class="action-btn btn-save" onclick="saveOpportunity(<%= lic.id %>)"
                                    title="Salvar">
                                    <i class="fas fa-heart"></i>
                                </button>
                            </div>

                            <!-- Loader (JS toggles this) -->
                            <div class="infinite-loader"></div>
                        </div>
                        <% }) %>
                            <% } %>
        </div>
    </div>

    <link rel="stylesheet" href="/css/dopamine.css">
    <script src="/js/story-feed.js" defer></script>